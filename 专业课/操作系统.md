# 一. 操作系统概述

## 1.功能

![image-20260315102555669](https://raw.githubusercontent.com/Sanshi2006/ImagesResources/main/img/image-20260315102555669.png)



## 2.特征

①并发：指两个或多个事件在同一时间间隔内发生。这些事件宏观上是同时发生的，但微观上是交替发生的。

易混概念——并行：两个或多个事件在同一时刻内发生

单核CPU同一时刻只能执行一个程序，各个程序只能并发地执行

多核CPU同一时刻可用同时执行多个程序，多个程序可以并行地执行



②共享：即资源共享，指系统中的资源可供内存中多个并发执行的进程同时使用

互斥共享方式：系统中的某些资源，可提供给多个进程使用，但一个时间段内只允许一个进程访问该资源

同时共享方式：系统中的某些资源，允许一个时间段内由多个进程“同时”对它们进行访问



③虚拟：指把一个物理上的实体变为若干个逻辑上的对应物，物理实体是实际存在的，逻辑对应物是用户感受到的



④异步：在多道程序环境下，允许多个程序并发执行，但由于资源有限，进程的执行不是一贯到底的，而是走走停停，以不可预知的速度向前推进。



## 3.操作系统的发展和分类

![image-20260315110156658](https://raw.githubusercontent.com/Sanshi2006/ImagesResources/main/img/image-20260315110156658.png)



## 4.运行机制

* CPU的两种状态：

①内核态：运行内核程序，可以执行特权指令

②用户态：运行应用程序，只能执行非特权指令

CPU中有一个寄存器为程序状态字寄存器（PSW），其中有个二进制位用于表示是内核态还是用户态



内核态 $\rightarrow$ 用户态：执行一条特权指令——修改PSW的标志位为“用户态”，这个动作意味着操作系统主动让出CPU使用权

用户态 $\rightarrow$ 内核态：由“中断”触发， 硬件自动完成，触发中断信号意味着操作系统将强行夺回CPU使用权



## 5.中断和异常

中断分为内中断和外中断

内中断：与当前执行的指令有关，中断信号来源于CPU内部

外中断：与当前执行的指令无关，中断信号来源于CPU外部



内中断例子：

①在用户态下执行特权指令

②执行除法指令时除数为0

③如果应用程序想请求操作系统内核的服务，此时会执行一条特殊的指令——陷入指令，该指令会引发一个内部中断信号



外中断例子：

①时钟中断——由时钟部件发来的中断信号



## 6.系统调用

![image-20260315153642868](https://raw.githubusercontent.com/Sanshi2006/ImagesResources/main/img/image-20260315153642868.png)

![image-20260315153825590](https://raw.githubusercontent.com/Sanshi2006/ImagesResources/main/img/image-20260315153825590.png)
